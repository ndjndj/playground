version: '3.7' 

services: 
  meili-dump:
    build: 
      context: .
    ports:
      - "9000:8080"
    environment: 
      - MEILI_MASTER_KEY=meili-master-key
      - MEILI_ENV=development
    volumes: 
      - ./meili_data/data.ms:/tmp/data.ms 
      - ./meili_data/dumps:/dumps